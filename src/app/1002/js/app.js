define(function(require) {
	// var tpl = require('../tpl/t.tpl');
	// console.log(ejs.render(tpl,{msg:'hello world!'}));

	// var loading = require('loading');
	// loading.open();

	// alert('快捷支付，余额，余额宝资金安全均由平安保险全额承保，发生风险100%赔付，赔付金额无上限');
	// setTimeout(function() {
	// 	alert(23234)
	// },3000)


	confirm('确定吗',function(b) {
		if(b) {
			alert("ok");
		}
	});

});